# What this repo is

Just-for-me plain-text configs. Symlinked into `~/.config` by
home-manager from `nixos` repo.

Same convention as `yurice`: `config/` mirrors `~/.config`.

- `config/git/config`        — git identity
- `config/opencode`          — opencode
